<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>de.jotschi.ai</groupId>
	<artifactId>dataset-processor</artifactId>
	<version>0.0.1-SNAPSHOT</version>


	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<compiler-plugin.version>3.14.0</compiler-plugin.version>
	</properties>


	<dependencies>
		<!-- Dataset support -->
		<dependency>
			<groupId>org.apache.arrow</groupId>
			<artifactId>arrow-dataset</artifactId>
			<version>18.3.0</version>
		</dependency>
		<dependency>
			<groupId>org.apache.arrow</groupId>
			<artifactId>arrow-memory-netty</artifactId>
			<version>18.3.0</version>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>me.tongfei</groupId>
			<artifactId>progressbar</artifactId>
			<version>0.10.0</version>
		</dependency>

		<dependency>
			<groupId>io.metaloom.utils</groupId>
			<artifactId>genai-utils</artifactId>
			<version>0.0.1-SNAPSHOT</version>
		</dependency>
		<dependency>
			<groupId>io.metaloom.utils</groupId>
			<artifactId>hash-utils</artifactId>
			<version>0.5.0-SNAPSHOT</version>
		</dependency>

		<!--Test -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter-engine</artifactId>
			<version>5.14.1</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<version>3.25.1</version>
			<scope>test</scope>
		</dependency>

	</dependencies>

	<build>
		<plugins>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${compiler-plugin.version}</version>
				<configuration>
					<!-- 21, not 24: the gpu-sandbox image ships Temurin 21 and 25. -->
					<release>21</release>
				</configuration>
			</plugin>
			<plugin>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.5.2</version>
				<configuration>
					<!--
						Most classes under src/test are ETL jobs wearing @Test, not unit
						tests: they need a live LLM endpoint and several loop forever.
						Only the offline stage-3 converter tests run by default.
						Run an ETL job explicitly, e.g.
						  mvn test -Dtest=StoryGeneratorTest -DfailIfNoTests=false
					-->
					<excludes>
						<exclude>**/StoryGeneratorTest.java</exclude>
						<exclude>**/StoryProcessorTest.java</exclude>
						<exclude>**/StoryBeginningListGeneratorTest.java</exclude>
						<exclude>**/QuestionBeginningListGeneratorTest.java</exclude>
						<exclude>**/AnfrageGeneratorTest.java</exclude>
						<exclude>**/QAGeneratorTest.java</exclude>
						<exclude>**/ParquetProcessorTest.java</exclude>
						<exclude>**/JsonlHasherTest.java</exclude>
						<exclude>**/TranslateProcessorTest.java</exclude>
					</excludes>
					<argLine>--add-opens=java.base/java.nio=ALL-UNNAMED</argLine>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>